{
    "builds": [{
        "src": "cross_backend/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.9" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "cross_backend/wsgi.py"
        }
    ]
}

